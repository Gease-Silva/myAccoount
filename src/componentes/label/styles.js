import { StyleSheet } from "react-native";

export default StyleSheet.create({
    titulo: {
        color: '#fff',
        fontSize: 24,
        fontWeight: 700,
    },
    subTitulo: {
        color: '#fff',
        fontSize: 16,
        fontWeight: 400,
    },
    corpo: {
         color: '#fff',
         fontSize: 12,
         fontWeight: 400,
    },
    corpoBlack: {
        color: '#000',
        fontSize: 16,
        fontWeight: 400,
   }
});
