import { StyleSheet } from "react-native";

export default StyleSheet.create({

    NavBar: {
        flexDirection: 'row',
        alignItems: 'center',
        justifyContent: 'space-around',
        borderRadius: 100,
        width: 'auto',
        height: 50,
        marginTop: 'auto',
    }

})
